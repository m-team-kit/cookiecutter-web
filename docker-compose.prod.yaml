version: "3.5"

services:
  frontend:
    build:
      target: production

  backend_v1:
    build:
      target: production
    environment:
      FLASK_DEBUG: "False"
      GUNICORN_WORKERS: 1

  reverse_proxy:
    build:
      target: production
